//
// Theme variables and color palette for the EMS Frontend
//

// PUBLIC_INTERFACE
export const COLORS = {
  primary: "#1976d2",     // main blue
  secondary: "#424242",   // deep grey
  accent: "#ffb300",      // accent yellow
  bg: "#f8fafc",          // very light background
  text: "#23232a",        // primary text color
  sidebar: "#f5f5f7",
  nav: "#fff",
  border: "#e3e3e3",
};

export const themeVars = {
  "--primary": COLORS.primary,
  "--secondary": COLORS.secondary,
  "--accent": COLORS.accent,
  "--bg": COLORS.bg,
  "--text": COLORS.text,
  "--sidebar": COLORS.sidebar,
  "--nav": COLORS.nav,
  "--border": COLORS.border,
};
